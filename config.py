"""
config.py — все настройки Али
"""

from pathlib import Path

# Корень проекта (папка где лежит config.py)
BASE_DIR = Path(__file__).parent

# =============================================================================
# Gemini
# =============================================================================
GEMINI_API_KEY = "AIzaSyBrUc7xoM83VEfx3V_bpqWT_NRC0iZ1Kxg"
GEMINI_MODEL = "gemini-2.0-flash"
GEMINI_MIN_INTERVAL = 4.0   # минимум секунд между запросами (rate limit)

# =============================================================================
# Whisper
# =============================================================================
WHISPER_MODEL = "base"          # tiny / base / small / medium
WHISPER_DEVICE = "cuda"         # "cuda" или "cpu"
WHISPER_COMPUTE_TYPE = "int8_float16"  # int8_float16 для GTX 1080 (Pascal не поддерживает чистый float16)

# =============================================================================
# Wake-word
# =============================================================================
WAKE_WORD = "аля"
# Regex учитывает транслитерацию Whisper (иногда пишет латиницей)
WAKE_WORD_PATTERN = r"[аa][лl][яei]"

# =============================================================================
# Запись звука
# =============================================================================
SAMPLE_RATE = 16000
CHANNELS = 1
VAD_THRESHOLD = 0.02            # RMS порог начала речи (0.01 тихий, 0.05 громкий)
VAD_SILENCE_DURATION = 1.5     # секунд тишины до остановки записи
CHUNK_DURATION = 0.1            # длина одного чанка (сек)

# =============================================================================
# Экран
# =============================================================================
SCREEN_W = 2560
SCREEN_H = 1440
SCREENSHOT_W = 1280
SCREENSHOT_H = 720

# =============================================================================
# Edge TTS
# =============================================================================
EDGE_TTS_VOICE = "ru-RU-DariyaNeural"

# =============================================================================
# RVC
# =============================================================================
RVC_ENABLED = False   # требует Microsoft C++ Build Tools; включи после установки
RVC_MODEL_PATH = str(BASE_DIR / "models" / "rvc" / "voice.pth")
RVC_INDEX_PATH = str(BASE_DIR / "models" / "rvc" / "voice.index")
RVC_PITCH = 0           # 0 = без изменений; +6 = аниме-голос выше
RVC_METHOD = "rmvpe"    # rmvpe — самый качественный алгоритм
RVC_INDEX_RATE = 0.75

# =============================================================================
# VTube Studio
# =============================================================================
VTS_HOST = "ws://localhost:8001"
VTS_PLUGIN_NAME = "Аля Ассистент"
VTS_PLUGIN_DEV = "user"
VTS_TOKEN_FILE = str(BASE_DIR / ".vts_token")

# =============================================================================
# Персональность
# =============================================================================
VOICE_RESPONSE = True
ALY_PERSONALITY = """
Ты — Аля, аниме-ассистент с белыми волосами.
Отвечаешь коротко, по делу, иногда с лёгким аниме-флёром.
Обращаешься к пользователю на "ты".
Ответы максимум 2 предложения — ты голосовой ассистент, не чат-бот.
"""

# =============================================================================
# Логирование
# =============================================================================
LOG_FILE = str(BASE_DIR / "aly.log")
LOG_MAX_BYTES = 5 * 1024 * 1024    # 5 MB
LOG_BACKUP_COUNT = 3
